document.addEventListener("DOMContentLoaded",()=>{const search=document.getElementById("serviceSearch");if(search){search.addEventListener("input",()=>{const q=search.value.toLowerCase();document.querySelectorAll(".service-item").forEach(x=>x.style.display=x.innerText.toLowerCase().includes(q)?"":"none")});}document.querySelectorAll("form").forEach(f=>f.addEventListener("submit",e=>{if(!f.checkValidity()){e.preventDefault();f.classList.add("was-validated");}}));document.querySelectorAll("form[data-confirm-message]").forEach(f=>f.addEventListener("submit",e=>{if(f.dataset.confirmed){return;}e.preventDefault();if(f.querySelector(".delete-confirmation")){return;}const box=document.createElement("div");box.className="alert alert-warning delete-confirmation text-start mt-2 mb-0";box.innerHTML=`<span>${f.dataset.confirmMessage}</span> <button type="button" class="btn btn-sm btn-danger ms-2">Confirm</button> <button type="button" class="btn btn-sm btn-outline-secondary">Cancel</button>`;f.appendChild(box);box.querySelector(".btn-danger").addEventListener("click",()=>{f.dataset.confirmed="true";f.submit();});box.querySelector(".btn-outline-secondary").addEventListener("click",()=>box.remove());}));document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener("click",e=>{const el=document.querySelector(a.getAttribute("href"));if(el){e.preventDefault();el.scrollIntoView({behavior:"smooth"});}}));});